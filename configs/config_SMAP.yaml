data:
  dataset_name: SMAP
  data_path: ./data/MSL/
  window_size: 24
  step_size: 2

experiment:
  name: exp_SMAP

training:
  batch_size: 64
  shuffle: true
  learning_rate: 0.001
  num_epochs: 60
  device: cuda
  weight_decay: 1e-5
  rec: 1.0
  con: 0.02
  adv: 0.1

dbpm:
  beta_np: 1.0
  beta_fp: 1.0
  warmup_epochs: 5

anomaly:
  num_anomalies_per_sample: 8
  noise_sigma: 0.05

contrastive:
  temperature: 0.1
  recon_margin: 0.5

evaluation:
  gmm_components: 0
  score_level: point
  smoothing:
    method: median
    kernel: 1
  score_postprocess:
    mode: close_open
    kernel: 17
    fixed_alpha: 0.9

model:
  encoder:
    input_dim: 25
    proj_dim: 28
    hidden_dim: 12
    nhead: 4
    num_layers: 4
    dim_feedforward: 128
    dropout: 0.1
  decoder:
    hidden_dim: 12
    output_dim: 25
  discriminator:
    hidden_dim: 25
    output_dim: 1
